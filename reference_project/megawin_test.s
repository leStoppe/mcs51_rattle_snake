.title MainFile
.module main

.include /asxxxx_REG_MG72F6D17.s/

.area VECTORS (ABS,OVR,CSEG)
	.org 0x00
LJMP START

.area CODE (REL,CON,CSEG)
START:	
	MOV A, 0x20
	MOV A, #0d20
	;MOV dptr, 20


	MOV IRH_VAR1, A
	MOV A, IRH_VAR1
	PUSH IRH_VAR1
	XCH A, IRH_VAR1
	CJNE A, IRH_VAR1, START
	CJNE A, IRL_VAR1, START
	MOV @r1, IRH_VAR1
	mov @r1, IRL_VAR1
	MOV IRL_VAR1, IRH_VAR1
	MOV IRH_VAR1, IRL_VAR1

	MOV A, SBIT_TCON.7
	MOV IRL_VAR1, XR_VAR1
	ORL C, /BIT_VAR1

	MOV R2, #IRH_VAR1
	SETB SBIT_TCON.6
	SETB IRH_VAR1
	MOV C, SBIT_TCON.6
	MOV C, SBYTE_P1

	ACALL test_call
	1$: SJMP START
	2$: SJMP 1$
	SJMP 2$

.end